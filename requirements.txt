# Core bot
python-telegram-bot[job-queue]>=21,<23

# Claude API
anthropic>=0.40

# Config
python-dotenv>=1.0

# Google (Calendar read + Gmail read) — used from Session 6 on
google-api-python-client>=2.140
google-auth>=2.34
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Weather + generic HTTP — used from Session 7 on
httpx>=0.27
